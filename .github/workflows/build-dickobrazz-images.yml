name: 🚀 Build and Push Dickobrazz Images

on:
  push:
    branches: [ master ]
    paths:
      - 'versions/.version.dickobrazz-bot'
      - 'versions/.version.dickobrazz-bot-test'
      - 'versions/.version.dickobrazz-grafana'
      - 'versions/.version.dickobrazz-prometheus'
      - 'versions/.version.dickobrazz-server'
      - 'versions/.version.dickobrazz-server-test'
      - 'docker-specs/Dockerfile.dickobrazz-bot'
      - 'docker-specs/Dockerfile.dickobrazz-bot-test'
      - 'docker-specs/Dockerfile.dickobrazz-grafana'
      - 'docker-specs/Dockerfile.dickobrazz-prometheus'
      - 'docker-specs/Dockerfile.dickobrazz-server'
      - 'docker-specs/Dockerfile.dickobrazz-server-test'
      - 'configurations/dickobrazz-bot/config.dickobrazz-bot.yaml'
      - 'configurations/dickobrazz-bot/config.dickobrazz-bot-test.yaml'
      - 'configurations/dickobrazz-server/config.dickobrazz-server.yaml'
      - 'configurations/dickobrazz-server/config.dickobrazz-server-test.yaml'
      - 'configurations/grafana/grafana-datasources.yaml'
      - 'configurations/grafana/grafana-dashboards.yaml'
      - 'configurations/grafana/grafana-dashboard-dickobrazz.json'
      - 'configurations/prometheus/prometheus.yml'
      - '.github/workflows/build-dickobrazz-images.yml'

permissions:
  contents: write
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  ORG: mairwunnx-infra
  PLATFORMS: linux/amd64

jobs:
  build:
    name: 🔨 Build & push (${{ matrix.display_name }})
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    strategy:
      fail-fast: false
      matrix:
        include:
          - display_name: Dickobrazz Bot
            image_name: dickobrazz-bot
            dockerfile: docker-specs/Dockerfile.dickobrazz-bot
            version_file: versions/.version.dickobrazz-bot
            watch_files: |
              versions/.version.dickobrazz-bot
              docker-specs/Dockerfile.dickobrazz-bot
              configurations/dickobrazz-bot/config.dickobrazz-bot.yaml
              .github/workflows/build-dickobrazz-images.yml
          - display_name: Dickobrazz Bot Test
            image_name: dickobrazz-bot-test
            dockerfile: docker-specs/Dockerfile.dickobrazz-bot-test
            version_file: versions/.version.dickobrazz-bot-test
            watch_files: |
              versions/.version.dickobrazz-bot-test
              docker-specs/Dockerfile.dickobrazz-bot-test
              configurations/dickobrazz-bot/config.dickobrazz-bot-test.yaml
              .github/workflows/build-dickobrazz-images.yml
          - display_name: Dickobrazz Server
            image_name: dickobrazz-server
            dockerfile: docker-specs/Dockerfile.dickobrazz-server
            version_file: versions/.version.dickobrazz-server
            watch_files: |
              versions/.version.dickobrazz-server
              docker-specs/Dockerfile.dickobrazz-server
              configurations/dickobrazz-server/config.dickobrazz-server.yaml
              .github/workflows/build-dickobrazz-images.yml
          - display_name: Dickobrazz Server Test
            image_name: dickobrazz-server-test
            dockerfile: docker-specs/Dockerfile.dickobrazz-server-test
            version_file: versions/.version.dickobrazz-server-test
            watch_files: |
              versions/.version.dickobrazz-server-test
              docker-specs/Dockerfile.dickobrazz-server-test
              configurations/dickobrazz-server/config.dickobrazz-server-test.yaml
              .github/workflows/build-dickobrazz-images.yml
          - display_name: Dickobrazz Grafana
            image_name: dickobrazz-grafana
            dockerfile: docker-specs/Dockerfile.dickobrazz-grafana
            version_file: versions/.version.dickobrazz-grafana
            watch_files: |
              versions/.version.dickobrazz-grafana
              docker-specs/Dockerfile.dickobrazz-grafana
              configurations/grafana/grafana-datasources.yaml
              configurations/grafana/grafana-dashboards.yaml
              configurations/grafana/grafana-dashboard-dickobrazz.json
              .github/workflows/build-dickobrazz-images.yml
          - display_name: Dickobrazz Prometheus
            image_name: dickobrazz-prometheus
            dockerfile: docker-specs/Dockerfile.dickobrazz-prometheus
            version_file: versions/.version.dickobrazz-prometheus
            watch_files: |
              versions/.version.dickobrazz-prometheus
              docker-specs/Dockerfile.dickobrazz-prometheus
              configurations/prometheus/prometheus.yml
              .github/workflows/build-dickobrazz-images.yml

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Detect changes for ${{ matrix.display_name }}
        id: changes
        shell: bash
        run: |
          set -euo pipefail

          CHANGED_FILES="$(git diff --name-only HEAD~1 HEAD || true)"

          version_changed=false
          image_changed=false

          while IFS= read -r changed_file; do
            [ -z "$changed_file" ] && continue

            if [ "$changed_file" = "${{ matrix.version_file }}" ]; then
              version_changed=true
            fi

            while IFS= read -r watch_file; do
              [ -z "$watch_file" ] && continue
              if [ "$changed_file" = "$watch_file" ]; then
                image_changed=true
              fi
            done <<< "${{ matrix.watch_files }}"
          done <<< "$CHANGED_FILES"

          build=false
          if [ "$image_changed" = "true" ] && [ "$version_changed" = "true" ]; then
            build=true
          fi

          echo "image_changed=$image_changed" >> "$GITHUB_OUTPUT"
          echo "version_changed=$version_changed" >> "$GITHUB_OUTPUT"
          echo "build=$build" >> "$GITHUB_OUTPUT"

      - name: Read version
        id: version
        if: steps.changes.outputs.build == 'true'
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(cat "${{ matrix.version_file }}")"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-qemu-action@v3
        if: steps.changes.outputs.build == 'true'

      - uses: docker/setup-buildx-action@v3
        if: steps.changes.outputs.build == 'true'

      - name: Login to GHCR
        if: steps.changes.outputs.build == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Meta
        id: meta
        if: steps.changes.outputs.build == 'true'
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.ORG }}/${{ matrix.image_name }}
          tags: |
            type=raw,value=${{ steps.version.outputs.version }},enable=true
            type=sha,prefix=sha-,format=short

      - name: Build & Push
        if: steps.changes.outputs.build == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          platforms: ${{ env.PLATFORMS }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          no-cache: true

      - name: Skip summary
        if: steps.changes.outputs.build != 'true'
        run: |
          echo "Skipping ${{ matrix.display_name }}:"
          echo "- image_changed=${{ steps.changes.outputs.image_changed }}"
          echo "- version_changed=${{ steps.changes.outputs.version_changed }}"
